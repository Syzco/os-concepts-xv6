#include "types.h"
#include "stat.h"
#include "user.h"
#include "fs.h"


int
main(int argc, char *argv[])
{
  printf(1, "I am process with pid - %d\n", getpid());
  printf(1, "I am process with pid - %d\n", getpid());
  printf(1, "I am process with pid - %d\n", getpid());
  printf(1, "I am process with pid - %d\n", getpid());

  exit();
}
